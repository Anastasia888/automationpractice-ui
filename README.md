### How to run UI tests with Maven:
###### mvn clean compile && mvn clean test -P chrome
###### mvn clean compile && mvn clean test -P firefox
##### After running tests reports generate in folder target/surefire-reports

